import Sidebar from "../components/Sidebar";

const Home = () => {
    return (
        <div className="h-screen flex flex-col">
            <Sidebar />
        </div>
    );
};

export default Home;
